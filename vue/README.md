# Vue.js Implementation

This directory contains the Vue.js version of the WebDevComparison apps.

## 🚀 Quick Start

1.  **Install dependencies**:
    ```bash
    npm install
    ```

2.  **Run the development server**:
    ```bash
    npm run dev
    ```
    Open [http://localhost:5173](http://localhost:5173) in your browser.

3.  **Build for production**:
    ```bash
    npm run build
    ```

## 🛠️ Tech Stack

*   **Framework**: Vue.js 3 (Composition API)
*   **Build Tool**: Vite
*   **Language**: TypeScript
*   **Routing**: Vue Router
*   **State Management**: Reactivity API (`ref`, `computed`)

## 📱 Apps

*   **World Clock**: `/world-clock`
*   **Timer**: `/timer`
*   **Countdown**: `/countdown`

## 📝 Notes

*   Scaffolded using `npm create vite@latest vue -- --template vue-ts`.
*   Uses `<script setup>` syntax for concise component logic.
*   Scoped CSS for component-level styling.
